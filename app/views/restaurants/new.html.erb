<h2>Add new restaurant</h2>

<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.select :category, Restaurant::CATEGORIES %>
  <%= f.submit %>
<% end %>
<br>
<%= link_to "↩️ Back to list of all restaurants", restaurants_path(@restaurant) %>
